{
  "name": "underdark",
  "version": "1.0.0",
  "description": "Blog personal alternativo con estilo oscuro",
  "private": true,
  "scripts": {
    "build": "echo 'Static site - no build needed'",
    "dev": "wrangler pages dev public --kv POSTS",
    "deploy": "wrangler pages deploy public",
    "create-kv": "wrangler kv namespace create POSTS"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
